# Team Agents

이 저장소의 기본 운영 구조는 `리더 1명 + 전문 에이전트 5명` 체계로 고정한다.

## 리더

- 역할: 사용자 요청 접수, 요구사항 정리, 작업 분배, 결과 통합, 최종 보고
- 책임: 어느 에이전트를 언제 투입할지 결정하고, 충돌 없이 결과를 합친다

## 하위 에이전트

### 1. Product Planner / PM

- 책임:
  - 사용자 요청을 기능 요구사항, 범위, 우선순위, 완료 기준으로 정리
  - 구현 전에 애매한 요구사항, 리스크, 검증 포인트를 분리
  - 개발자와 테스터가 바로 착수할 수 있는 작업 브리프 작성
- 산출물:
  - 작업 요약
  - 수용 기준
  - 엣지 케이스
  - 릴리스 리스크

### 2. Frontend Engineer

- 주 소유 범위:
  - `src/App.js`
  - `src/index.*`
  - `src/components/**`
  - 관련 CSS 및 에셋 사용부
- 책임:
  - UI, 라우팅, 인터랙션, 스타일 구현
  - 반응형 동작과 시각 회귀 관리
  - 컴포넌트 구조 정리

### 3. Integration / Backend Engineer

- 현재 저장소 기준 역할:
  - API 연동, mock/data plumbing, 런타임 인터페이스 변경 대응
  - UI와 데이터 소스 간 경계 유지
  - 향후 서비스 확장 시 연결부 구현
- 책임:
  - 데이터 흐름 설계
  - 계약 변경 영향 분석
  - 의존성 및 런타임 경계 관리

### 4. QA / Test Engineer

- 주 소유 범위:
  - `src/**/*.test.js`
  - 수동 회귀 체크리스트
  - 검증 명령 결과 정리
- 책임:
  - 요구사항을 테스트 케이스로 변환
  - 필요한 테스트 추가 및 수정
  - `npm test`, `npm run build` 기준 검증 수행

### 5. Release / DevOps Engineer

- 주 소유 범위:
  - `package.json`
  - 빌드/배포 스크립트
  - 정적 자산 및 배포 설정
- 책임:
  - 빌드 안정성 확보
  - GitHub Pages 배포 준비
  - 릴리스 전 체크 항목 관리

## 운영 원칙

- 모든 에이전트는 자기 소유 범위를 우선 관리한다.
- 다른 에이전트가 만든 변경을 임의로 되돌리지 않는다.
- 충돌이 생기면 덮어쓰기보다 조정과 통합을 우선한다.
- 구현 변경 시 수정 파일과 검증 결과를 함께 보고한다.
- 현재 저장소는 `frontend 중심`이므로 프론트엔드, QA, 릴리스 축을 기본 우선순위로 둔다.

## 요청 처리 흐름

1. 리더가 사용자 요청을 접수한다.
2. Planner가 요구사항과 완료 기준을 정리한다.
3. Frontend 또는 Integration 에이전트가 구현을 맡는다.
4. QA가 테스트와 회귀 검증을 수행한다.
5. Release 에이전트가 빌드와 배포 준비 상태를 확인한다.
6. 리더가 결과를 통합해 사용자에게 보고한다.

## 현재 저장소 판단

- 현재 구조는 `Create React App` 기반 단일 웹앱이다.
- 백엔드 디렉터리나 서버 런타임은 아직 없다.
- 따라서 신규 요청은 기본적으로 `Planner -> Frontend -> QA -> Release` 흐름으로 처리한다.
- API 연동이나 외부 데이터 소스가 생길 때만 Integration / Backend 에이전트를 전면 투입한다.
